apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: testfordev-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/devops-champ/testfordev.git
    targetRevision: HEAD
    path: helm
    helm:
      valueFiles:
        - ../gitops-nonprod/dev/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: testfordev-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
