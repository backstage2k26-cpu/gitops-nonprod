apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: https-test-dev
  namespace: argocd

spec:
  project: default

  sources:
    - repoURL: https://github.com/devops-champ/https-test.git
      targetRevision: HEAD
      path: helm/app

      helm:
        valueFiles:
          - $values/https-test/values/values-dev.yaml

    - repoURL: https://github.com/backstage2k26-cpu/gitops-nonprod.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: https-test-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true