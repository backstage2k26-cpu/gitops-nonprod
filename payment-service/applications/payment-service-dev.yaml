apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: payment-service-dev
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/devops-champ/payment-service.git
      targetRevision: 0.0.2
      path: helm/app
      helm:
        valueFiles:
          - $values/payment-service/values/values-dev.yaml
    - repoURL: https://github.com/backstage2k26-cpu/gitops-nonprod.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: payment-service-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
