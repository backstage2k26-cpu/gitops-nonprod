apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: user-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/devops-champ/user.git
    targetRevision: HEAD
    path: helm
    helm:
      valueFiles:
        - ../../values/user/values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: user-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
