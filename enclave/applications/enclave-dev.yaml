apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: enclave-dev
  namespace: argocd

spec:
  project: default

  sources:
    - repoURL: https://github.com/devops-champ/enclave.git
      targetRevision: HEAD
      path: helm/app

      helm:
        valueFiles:
          - $values/enclave/values/values-dev.yaml

    - repoURL: https://github.com/backstage2k26-cpu/gitops-nonprod.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: enclave-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true