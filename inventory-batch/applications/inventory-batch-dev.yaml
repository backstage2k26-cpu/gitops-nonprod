apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: inventory-batch-dev
  namespace: argocd

spec:
  project: default

  sources:
    - repoURL: https://github.com/devops-champ/inventory-batch.git
      targetRevision: HEAD
      path: helm/app

      helm:
        valueFiles:
          - $values/inventory-batch/values/values-dev.yaml

    - repoURL: https://github.com/backstage2k26-cpu/gitops-nonprod.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: inventory-batch-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true